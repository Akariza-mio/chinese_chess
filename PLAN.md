# 中国象棋项目开发计划（9 月 1 日—9 月 21 日）

## 总体目标

在 Windows 11、Visual Studio 2026、C++20 环境下完成：

- 9 月 21 日必须完成稳定的 raylib 图形化本地双人对局。
- 同期冲刺简化局域网对战；若未达到稳定验收标准，延长至 9 月 26—28 日。
- 实现完整基础对局规则，但不实现长将、长捉、重复局面与复杂和棋裁定。
- 主要由你编写棋盘模型、规则和界面；我负责拆分任务、讲解知识、代码审查和调试。联网模块可在你明确要求后由我提供较完整实现。
- 执行阶段先把本计划写入新的 `中国象棋开发计划.md`，不覆盖原需求文档。
- 未经明确授权，不修改 `.slnx`、`.vcxproj`、`.filters`、`.user` 等 Visual Studio 文件，也不处理上级 Git 工作树中的无关改动。

总投入预计 47—63 小时。每天 2—3 小时属于可完成但较紧的安排，因此 9 月 14 日设置本地版硬检查点。

## 技术设计

### 核心模型

采用值语义，不使用拥有资源的裸指针：

- `Side`：红方、黑方。
- `PieceType`：将/帅、士、象、马、车、炮、兵/卒。
- `Position`：行、列；统一规定第 0 行为黑方底线，第 9 行为红方底线。
- `Piece`：类型与阵营。
- `Board`：`10 × 9` 的 `std::array`，格子使用 `std::optional<Piece>`。
- `Move`：起点、终点。
- `GameState`：棋盘、当前行棋方、对局状态和最后一步。
- `GameStatus`：进行中、红胜、黑胜、连接中断。

`RuleEngine` 负责：

1. 判断单个棋子的基本走法。
2. 判断蹩马腿、塞象眼、炮架、过河、九宫和将帅照面。
3. 判断某格是否受到攻击、某方是否被将军。
4. 在棋盘副本上试走，禁止走完后己方仍被将军。
5. 枚举合法着法并判断将死或困毙；无合法着法的一方判负。
6. 正式对局不以“吃掉将帅”结束，将帅始终保留到胜负裁定。

图形、网络都只能通过 `GameState` 和 `Move` 使用规则层，不能各自复制规则。

### 图形与游戏模式

- `GameApp` 管理 raylib 主循环和页面切换。
- `BoardRenderer` 只负责棋盘、河界、九宫、棋子、选中状态、合法落点、最后一步和提示信息。
- `IGameSession` 提供读取状态、提交走子、更新会话和重开对局的统一接口。
- `LocalSession` 在同一程序内轮流控制红黑双方。
- `NetworkSession` 将操作交给联网模块，但仍复用同一个规则引擎。

第一版界面包含主菜单、本地对局、创建主机、加入主机、IP 输入、当前行棋方、将军/胜负提示、重新开始和返回菜单。不加入动画、AI、计时器、聊天、悔棋和音频。

中文使用项目内可再分发的 Noto Sans CJK SC 字体及其许可证；通过 `LoadFontEx`/`DrawTextEx` 加载所需字符。资源一律相对可执行文件目录定位，交付时随 `assets` 一同复制。

raylib 默认使用当前稳定版 6.0 的 Windows x64 MSVC 包；该版本由官方提供 Windows/MSVC 构建产物，[官方发布页](https://github.com/raysan5/raylib/releases)和[Windows 构建配置](https://github.com/raysan5/raylib/blob/master/.github/workflows/build_windows.yml)均覆盖 x64 MSVC。由你在 VS 属性页配置头文件、库目录及链接库，我提供逐项指导。

### 简化局域网协议

使用 Windows 原生 Winsock2 和 TCP，不增加第三方网络库：

- 主机固定执红，客户端固定执黑；默认端口 `27015`。
- 主机监听 `0.0.0.0`；客户端输入 `127.0.0.1` 可在同机双开测试，输入局域网 IPv4 可在两台设备联机。
- 不实现房间大厅、UDP 自动发现、互联网穿透、观战、加密和断线续局。
- 主机是唯一权威端：客户端只提交走子请求，主机验证后广播完整状态。
- TCP 收发在后台线程进行；后台线程不得调用 raylib，只通过带互斥锁的消息队列与主线程通信。
- 使用 ASCII、换行分隔的文本消息：`HELLO`、`ROLE`、`MOVE`、`STATE`、`ERROR`、`QUIT`。
- `MOVE` 携带状态序号和四个坐标；`STATE` 携带序号、当前阵营、胜负状态及 90 格棋盘编码。
- 主机拒绝越界、过期、非当前方或非法走子，并返回最新状态；断线后结束当前会话并返回菜单，不自动重连。

## 日程与里程碑

| 日期 | 主要任务 | 当日验收 |
|---|---|---|
| 9/1 | 固化需求；学习 raylib 主循环；由你配置 raylib | 能打开、刷新并正常关闭窗口 |
| 9/2 | 绘制 9×10 棋盘、河界、九宫；加载中文字体 | 棋盘比例正确，中文无乱码 |
| 9/3 | 定义核心类型、棋盘坐标和初始局面 | 32 枚棋子位置正确 |
| 9/4 | 实现车、炮及路径阻挡 | 车与炮的移动/吃子测试通过 |
| 9/5 | 实现马、象 | 蹩马腿、塞象眼、象不过河通过 |
| 9/6 | 实现士、将帅、兵卒 | 九宫、过河和兵卒方向测试通过 |
| 9/7 | 实现攻击检测和将帅照面 | 能识别普通将军及飞将 |
| 9/8 | 实现试走与撤销，禁止送将 | 被牵制棋子不能暴露己方将帅 |
| 9/9 | 枚举合法着法，完善特殊解将场景 | 挡将、吃将军棋子、移帅均正确 |
| 9/10 | 实现将死、困毙和胜负状态 | 自定义残局能得到正确结果 |
| 9/11 | 集中编写规则回归测试并修复问题 | 核心自动测试全部通过 |
| 9/12 | 鼠标坐标转换、选子、落子、合法点提示 | 可在图形界面完成合法走子 |
| 9/13 | 当前方、将军、胜负、重开和菜单 | 本地模式形成完整闭环 |
| 9/14 | 完整本地实战与异常操作测试 | 本地版达到硬验收线 |
| 9/15 | 冻结规则接口；实现协议编码和 Winsock 生命周期 | 消息可序列化、解析并拒绝坏数据 |
| 9/16 | 主机监听、客户端连接、角色握手 | 同机两个窗口成功连接 |
| 9/17 | 客户端提交着法、主机权威验证、状态同步 | 双窗口可连续轮流走子 |
| 9/18 | 处理错误着法、重复消息、退出和断线 | 两端不会因异常消息崩溃 |
| 9/19 | 两台 Windows 设备真实局域网测试 | 输入主机 IPv4 后可完成多回合 |
| 9/20 | 全量回归、资源路径和 Release 打包 | 独立交付目录可运行 |
| 9/21 | 缓冲修复、最终验收、整理 README | 发布本地稳定版和已验证的 LAN 版本 |

进度门槛：

- 9 月 10 日前规则层必须基本完成。
- 9 月 14 日前本地完整对局必须稳定。
- 若 9 月 15 日开始时核心测试仍有阻断问题，暂停联网，把 9 月 21 日用于本地版修复；LAN 顺延至 9 月 26—28 日。
- 联网代码完成不等于联网验收完成，必须通过同机双开与真实双机两轮测试。

## 测试与验收

Debug 版本提供 `--self-test` 参数，在创建 raylib 窗口前运行轻量断言测试，避免引入测试框架或额外 VS 测试项目。

必须覆盖：

- 七类棋子的正常移动、边界、阻挡、吃子和阵营限制。
- 炮隔一子吃子、马腿、象眼、不过河、九宫和兵卒过河。
- 将帅照面、直接将军、闪将、双重攻击和被牵制棋子。
- 非法走子不得改变棋盘、当前方和状态序号。
- 合法走子后才切换行棋方。
- 将死、困毙以及能够挡将、吃攻击子或移动将帅的非将死局面。
- 鼠标点击棋盘边缘、空格、敌方棋子、窗口外区域。
- TCP 半包、多包合并、坏坐标、错误状态序号、抢行、断线。
- `127.0.0.1` 双窗口完整对局流程。
- 两台 Windows 11 设备通过局域网 IPv4 连接，必要时允许 Windows 防火墙放行程序。
- 将 Release 可执行文件与资源复制到干净目录后，字体和游戏均能正常运行。

## 协作方式与默认约束

- 每个阶段先由我解释目标、接口、相关 C++/raylib 知识和常见错误，再由你完成第一版代码。
- 你提交代码或错误信息后，我进行静态审查、定位原因并提出最小修改方案；只有收到明确命令才直接改文件或运行编译测试。
- 网络模块可由我起草完整代码，但在交付前会按类、线程、协议和资源生命周期逐段讲解，避免成为无法维护的黑盒。
- 所有编译均使用 VS2026 的 MSVC x64 Debug/Release；不切换 MinGW，不替换现有解决方案。
- 9 月 21 日的硬成果是完整基础规则的本地版；简化 LAN 是冲刺成果。复杂竞赛裁定、AI、自动发现、断线续局、动画和音频均留作后续版本。
- 最终双机 LAN 验收默认需要一台额外 Windows 设备；若届时无法提供，只能确认 localhost 联机，不能宣称真实局域网已经验证。
